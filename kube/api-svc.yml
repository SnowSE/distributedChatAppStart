apiVersion: v1
kind: Service
metadata:
  name: chat-app-api-service
  namespace: alex
spec:
  selector:
    app: chat-app-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
